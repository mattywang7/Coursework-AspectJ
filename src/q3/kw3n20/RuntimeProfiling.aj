package q3.kw3n20;

/**
 * "method signature"-hist.csv (one file for each method) using a comma separated format.
 * Each hist has three columns: value, inputTimes, returnTimes.
 *
 * failures.csv -- each line provides the failure frequency of a method (percentage of times).
 *
 * runtimes.csv -- one line for each method (average execution time, standard deviation across all its invocations).
 */
public aspect RuntimeProfiling {

}
